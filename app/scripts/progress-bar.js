export const progressAnim = ({ selector }) => {
  const progressBar = document.querySelector('.progress__bar')
  const progressBarFill = progressBar.querySelector('.progress__bar__fill')
  console.log(progressBar, progressBarFill)
}
